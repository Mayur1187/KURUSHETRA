/**
 * DOMAIN MODULE - REPLACE AFTER THE PROBLEM STATEMENT IS RELEASED.
 *
 * Anything specific to the actual hackathon problem (custom input
 * fields, special validation, request types, result shapes) should
 * be configured here rather than scattered through the core pages.
 */
export const domainConfig = {
  requestType: "generic", // e.g. "classification", "recommendation", "summarization"
  inputLabel: "Your input",
  resultLabel: "Result",
};

export default domainConfig;
